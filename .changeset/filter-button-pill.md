---
"@billabex/ui-components": patch
---

fix(ui-components): FilterButton uses pill shape and subtle shadow

Align with the prototype `.filter-chip`: `borderRadius: full` (was `sm`) and `boxShadow: sm`. No API change.
