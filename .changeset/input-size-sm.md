---
"@billabex/ui-components": minor
---

feat(ui-components): add `size="sm"` to Input for list-panel search fields

- `size="md"` (default) — unchanged (14/20, 8/8 padding).
- `size="sm"` — 13/18 (`body.sm`), 8/12 padding (`py: md`, `px: lg`). Matches the panel search field spec (`.task-search`, `.account-search`).
