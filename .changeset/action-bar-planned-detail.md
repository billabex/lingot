---
"@billabex/ui-components": minor
---

Add `ActionBar` layout primitive and `PlannedCommDetail` reference content. Simplify `TableRowDetail` API: remove `visible` and `density` props (consumers conditionally mount; detail padding is now constant). Rename `CommDetailExample` to `CommDetail`.
