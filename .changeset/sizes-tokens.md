---
"@billabex/ui-preset": minor
"@billabex/ui-components": patch
---

Add `sizes` token scale (xs → 5xl) mirroring `spacing`, so `width`/`height`/`minHeight` can reference the same semantic scale. Update `EmptyState` icon to use `3xl` tokens and refine typography (title `body`/regular, description `body.sm`/text.secondary, icon 32×32 text.tertiary with stroke 1.5). Relax `EmptyState` default padding from `4xl` to `2xl`.
